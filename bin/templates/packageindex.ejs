{
  "name": "<%= packageName %>",
  "version": "1.0.0",
  "description": "一个快捷创建前端项目的自定义脚手架",
  "main": "index.js",
  "scripts": {
    "dev": "node index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wzq-taxun/webProjects-cli.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "koa": "^2.13.1"
    <% if (middleware.router) { %>
          ,"koa-router": "^10.0.0"
      <% } %>
    
    <% if (middleware.views) { %>
          ,"koa-views": "^7.0.1"
          ,"pug": "^3.0.2"
      <% } %>
    
    <% if (middleware.body) { %>
          ,"koa-body": "^4.1.3"
      <% } %>
    
    <% if (middleware.static) { %>
          ,"koa-static": "^5.0.0"
      <% } %>
  }
}
